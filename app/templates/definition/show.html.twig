{% extends 'base.html.twig' %}

{% block title %}Definition{% endblock %}

{% block body %}
    <h1>Definition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ definition.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ definition.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_definition_index') }}">back to list</a>

    <a href="{{ path('app_definition_edit', {'id': definition.id}) }}">edit</a>

    {{ include('definition/_delete_form.html.twig') }}
{% endblock %}
